/* The program loads data from the file specified in the 
/* arguments, builds a heap. Then it prompts the user 
/* to choose to display the sorted data on the screen, 
/* save the data to a file (you need to enter a file name), 
/* or exit without saving.
